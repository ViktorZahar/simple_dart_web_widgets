name: simple_dart_web_widgets
description: Simple dart web widgets.
version: 0.0.3
publish_to: none

platforms:
  web:

environment:
  sdk: '^2.16.0'

#dependencies:

dev_dependencies:
  build_runner: ^2.1.7
  build_web_compilers:
